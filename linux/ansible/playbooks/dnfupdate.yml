- name: Aktualizacja Rocky Linux przez dnf
  hosts: all
  become: true
  

  tasks:
    - name: Update all packages
      ansible.builtin.dnf:
        name: "*"
        state: latest
        update_cache: true
